# ── Server (Node / Express) ─────────────────────────────────────────────────
# TMDB API key — server-side only, never use a VITE_ prefix for this
TMDB_API_KEY=your_tmdb_api_key_here

# Port the Express server listens on (defaults to 3000 if omitted)
PORT=3000
